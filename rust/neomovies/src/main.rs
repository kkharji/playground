use eyre::Result;

mod conn;
mod graph_ext;
mod models;

// use models::{Movie, Person};
// use neo4rs::query;

#[tokio::main]
async fn main() -> Result<()> {
    // let conn = conn::new().await?;

    Ok(())
}
